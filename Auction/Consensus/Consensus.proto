syntax = "proto3";

option go_package = "./Consensus";

package Consensus;

service Consensus {
  rpc Sync (ClientBid) returns (Ack);
}

message ClientBid {
  int32 Id = 1;
  int32 Bid = 2;
  int32 Timestamp = 3;
}
message Ack{
  string status = 1;
}